{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "mobile": "pnpm --filter @workspace/faithconnect run start",
    "mobile:clear": "pnpm --filter @workspace/faithconnect run start:clear",
    "mobile:tunnel": "pnpm --filter @workspace/faithconnect run start:tunnel",
    "admin": "pnpm --filter @workspace/admin run dev",
    "build": "pnpm -r --if-present build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
